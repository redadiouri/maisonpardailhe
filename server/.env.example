#############################################
# Exemple de fichier d'environnement pour le serveur
# Copiez ce fichier vers `server/.env` et remplacez les valeurs
#############################################

# Base de données MySQL
DB_HOST=127.0.0.1
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=maisonpardailhe

# Secret de session (utilisé par express-session)
SESSION_SECRET=change_me_to_a_random_secret

# Serveur
PORT=3001
NODE_ENV=development

# URL publique de l'application (pour les emails et redirections)
APP_URL=http://localhost:3001

# Timezone (IANA name) used for formatting dates in emails and the SPA.
# Example: TIMEZONE=Europe/Paris
TIMEZONE=Europe/Paris

# Stripe (paiement en ligne)
# 1. Créez un compte sur https://stripe.com
# 2. Récupérez vos clés API dans Dashboard → Developers → API keys
# 3. Pour le webhook, créez un endpoint dans Developers → Webhooks
#    URL: https://votre-domaine.com/api/payment/webhook
#    Événements: checkout.session.completed, payment_intent.payment_failed
STRIPE_SECRET_KEY=sk_test_VOTRE_CLE_SECRETE_ICI
STRIPE_PUBLISHABLE_KEY=pk_test_VOTRE_CLE_PUBLIQUE_ICI
STRIPE_WEBHOOK_SECRET=whsec_VOTRE_SECRET_WEBHOOK_ICI

# SMTP (optionnel) - pour envoyer des emails via nodemailer
# Laissez vide si vous n'utilisez pas l'envoi d'emails
# 
# Pour Gmail SMTP :
# 1. Activez la validation en 2 étapes sur votre compte Google
# 2. Créez un "Mot de passe d'application" : https://myaccount.google.com/apppasswords
# 3. Utilisez ce mot de passe d'application dans SMTP_PASS
#
# Configuration Gmail SMTP :
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre-mot-de-passe-application
FROM_ADDRESS="Maison Pardailhé <votre-email@gmail.com>"

# Exemple:
# DB_HOST=127.0.0.1
# DB_USER=myuser
# DB_PASSWORD=securepassword
# DB_NAME=maisonpardailhe
# SESSION_SECRET=RANDOM_STRING_OF_AT_LEAST_32_CHARS